
console.log("Hello World!");



// Inject our event handlers into global object for use elsewhere
//global.emitEvent = emitEvent;

function render() {
  console.log("Hello World from render!");

  //let html = renderApp(state);
  // if (document.body) {
  //   document.body.innerHTML = html;
  // }
}

render();

